name: E2E Tests

on:
   push:
      branches: [main]
   pull_request:
      branches: [main]

jobs:
   e2e:
      runs-on: ubuntu-latest
      environment: test
      steps:
         - uses: actions/checkout@v4

         - name: Setup Bun
           uses: oven-sh/setup-bun@v2
           with:
              bun-version: latest

         - name: Install dependencies
           run: bun install

         - name: Install Playwright browsers
           run: bunx playwright install --with-deps chromium

         - name: Link and apply migrations to test project
           env:
              SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
           run: |
              bunx supabase link --project-ref ${{ secrets.TEST_SUPABASE_PROJECT_REF }}
              bunx supabase db push --yes

         - name: Run Playwright tests
           env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.TEST_SUPABASE_PUBLISHABLE_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SUPABASE_SERVICE_ROLE_KEY }}
           run: bunx playwright test

         - name: Upload Playwright report
           if: failure()
           uses: actions/upload-artifact@v4
           with:
              name: playwright-report
              path: playwright-report/
              retention-days: 7
